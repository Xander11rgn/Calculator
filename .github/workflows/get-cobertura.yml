name: Get cobertura

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        dotnet-version: ['3.0', '3.1.x', '5.0.x' ]

    steps:
      - name: Build
        run: dotnet build
      - name: Test
        run: dotnet test —collect:"XPlat Code Coverage"
